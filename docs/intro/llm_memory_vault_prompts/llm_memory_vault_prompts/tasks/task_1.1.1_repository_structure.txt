# TASK 1.1.1: Create Repository Structure

## Context
Creating the foundation for the LLM Memory Vault project - a local-first AI memory system that aggregates and secures conversations from multiple LLM providers.

## Your Role
You are a Python project structure expert creating a well-organized, production-ready repository layout.

## Objective
Create complete directory structure with all necessary files for a Python package managed by Poetry.

## Instructions

### 1. Create Directory Structure
```
llm_memory_vault/
├── README.md
├── LICENSE
├── .gitignore
├── .editorconfig
├── pyproject.toml (placeholder - will be populated in next task)
├── vault/
│   ├── __init__.py
│   ├── config/
│   │   └── __init__.py
│   ├── ingestion/
│   │   └── __init__.py
│   ├── classification/
│   │   └── __init__.py
│   ├── sanitization/
│   │   └── __init__.py
│   ├── distillation/
│   │   └── __init__.py
│   ├── storage/
│   │   └── __init__.py
│   ├── retrieval/
│   │   └── __init__.py
│   ├── api/
│   │   └── __init__.py
│   ├── cli/
│   │   └── __init__.py
│   └── security/
│       └── __init__.py
├── tests/
│   ├── __init__.py
│   ├── fixtures/
│   │   └── __init__.py
│   └── README.md
└── config_examples/
    └── README.md
```

### 2. Create .gitignore
Include:
- Python artifacts: `__pycache__/`, `*.pyc`, `*.pyo`, `*.pyd`, `.Python`
- Virtual environments: `venv/`, `env/`, `.venv/`
- IDE files: `.vscode/`, `.idea/`, `*.swp`, `*.swo`
- Sensitive vault data: `vault_data/`, `*.db`, `*.key`, `.salt`, `.session`
- Build artifacts: `dist/`, `build/`, `*.egg-info/`
- Testing: `.pytest_cache/`, `.coverage`, `htmlcov/`
- Logs: `*.log`

### 3. Create README.md
Include:
```markdown
# LLM Memory Vault

A local-first, privacy-focused system for aggregating, securing, and searching conversations from multiple LLM providers.

## Features
- 🔒 **Privacy First**: All sensitive data encrypted locally
- 🔍 **Unified Search**: Search across ChatGPT, Claude, Perplexity, and more
- 🏷️ **Smart Classification**: Automatic tagging and organization
- 🛡️ **PII Protection**: Detect and redact sensitive information
- 📦 **Easy Import**: Simple CLI for importing conversation exports

## Status
🚧 **Phase 1: Foundation** - In Development

## Quick Start
```bash
# Initialize vault
vault init

# Import conversations
vault import chatgpt ~/Downloads/conversations.json

# Search and retrieve
vault list
vault show <conversation-id>
```

## Documentation
Coming soon in Phase 1 completion.

## License
MIT License - See LICENSE file
```

### 4. Create LICENSE (MIT)
Standard MIT license text with current year and "Rich [Your Last Name]" as copyright holder.

### 5. Create .editorconfig
```ini
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 4

[*.{yml,yaml,json}]
indent_size = 2

[*.md]
trim_trailing_whitespace = false
```

### 6. Add Module Docstrings
Each `__init__.py` should have:
```python
"""
<module_name> - <brief description>

Part of LLM Memory Vault.
"""

__version__ = "0.1.0"
```

Examples:
- `vault/__init__.py`: "LLM Memory Vault - Local-first AI memory system"
- `vault/security/__init__.py`: "Security module - Encryption and key management"
- `vault/ingestion/__init__.py`: "Ingestion module - LLM provider adapters"

### 7. Create Placeholder pyproject.toml
```toml
[tool.poetry]
name = "llm-memory-vault"
version = "0.1.0"
description = "Local-first AI memory system"
authors = ["Rich <your-email@example.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
# Dependencies will be added in Task 1.1.2

[tool.poetry.group.dev.dependencies]
# Dev dependencies will be added in Task 1.1.2

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
```

## Safety Constraints
- ✅ Use standard Python package layout
- ✅ Include proper .gitignore for sensitive files
- ✅ Add MIT license (open source)
- ✅ Clear documentation from the start

## Acceptance Criteria
- [ ] All directories exist with proper structure
- [ ] All packages have `__init__.py` files with docstrings
- [ ] .gitignore excludes sensitive vault files
- [ ] Can import vault package: `python -c "import vault"`
- [ ] README.md provides clear project overview
- [ ] LICENSE file present (MIT)
- [ ] .editorconfig configured for consistent formatting

## Output Format
Create all files and directories as specified. Ensure proper Python package structure.

## Next Task
Task 1.1.2 will populate pyproject.toml with all dependencies.
